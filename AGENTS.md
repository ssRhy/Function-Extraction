# 编码原则
本项目：基于 Vladimir Propp 的"故事形态学"理论，使用 LLM + 向量检索自动归纳故事的结构功能（Functions）。
## 1. 先思考，再动手

不要假设，不要隐藏困惑，主动暴露权衡取舍。

在开始实现之前：

- 明确说出你的假设。如果不确定，先问。
- 如果存在多种理解方式，列出来让用户选择，不要自己默默挑一个。
- 如果有更简单的方案，直接说。该反驳的时候就反驳。
- 如果有任何不清楚的地方，停下来，指出哪里不清楚，然后提问。

## 2. 简洁优先

用最少的代码解决问题，不做任何投机性开发。

- 不做用户没要求的功能。
- 只用一次的代码不要搞抽象。
- 没人要求的“灵活性”和“可配置性”不要加。
- 不可能发生的场景不需要错误处理。
- 不需要多种兼容 fallback 方式；选择最优最精简的一种即可。
- 不要自己随意增加代码。
- 保持代码精简。
- 使用 LangGraph 范式和官方的函数。
- 如果你写了 200 行但 50 行就能搞定，重写。
- 问自己：“一个资深工程师会不会觉得这太复杂了？”如果会，简化。
- 优先复用已有的功能或者函数或者接口
- 不要随意改变已有代码结构
- 定期删减冗余代码

## 3. 精准修改

只改必须改的，只清理自己造成的问题。

编辑现有代码时：

- 不要“顺手改进”旁边的代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有代码风格，即使你会用不同的写法。
- 如果发现不相关的死代码，提一嘴就好，不要删。

当你的修改产生了孤立代码时：

- 移除因为你的修改而变得无用的 import、变量、函数。
- 不要移除修改前就已经存在的死代码，除非用户要求。

检验标准：每一行改动都应该能直接追溯到用户的请求。

4.把一些突破性的工作进展同步到HANDOFF.md
5.只总结一些突破性或者有意义的提出反问/疑问/新思考；把用户的反问、疑问或关键思考追加到THINKING.md（项目思考历程）
